# Compiler Pass Generation

### Scope
* select toolchain or IR (MLIR, LLVM, TVM)
* define baseline kernels or benchmark suite
* define representation of optimization pass sequence and parameters
* integrate LLM for pass sequence generation
* compile -> run -> measure -> prompt refinement
* baseline vs LLM-generated testing


